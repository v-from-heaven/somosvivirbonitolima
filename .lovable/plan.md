El usuario quiere actualizar el color principal de la landing VivirBonito al verde limón `#8FBF26` (actualmente es un olive oscuro oklch(0.42 0.04 145)).\n\n## Alcance\n1. Actualizar `src/styles.css`: convertir `#8FBF26` a oklch y redefinir `--olive`, `--olive-deep` y `--primary` en `:root`. Ajustar `--primary-foreground` para garantizar contraste legible (texto oscuro sobre fondo verde brillante).\n2. Revisar componentes de la landing que usen clases `bg-olive`, `text-olive`, `bg-primary`, `text-primary` para confirmar que el cambio en tokens se propaga automáticamente vía Tailwind; no se editarán valores hardcodeados de color salvo que existan.\n3. Verificar build (`tsc` / Vite) para asegurar que no hay errores de tipo ni de compilación tras el cambio de tokens.